<p><b>商品名:</b><%= @item.name %></p>
<p><b>商品詳細:</b><%= @item.description %></p>
<p><b>価格:</b><%= @item.price %></p>
<p><b>出品者:</b><%= @item.seller %></p>
<p><b>電子メール:</b><%= @item.email %></p>
<p><%= image_tag @item.image_url %></p>

<!--編集のリンクを追加して、クリック後に編集画面にとぶように-->
<%= link_to "編集する",  edit_item_path(@item)%>
<%= link_to "削除",  item_path(@item), method: :delete, data: {confirm: "削除してよろしいですか"}%>